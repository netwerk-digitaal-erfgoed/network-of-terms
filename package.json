{
  "name": "network-of-terms",
  "private": true,
  "workspaces": [
    "./packages/*"
  ],
  "type": "module",
  "devDependencies": {
    "@qiwi/multi-semantic-release": "^3.17.1",
    "@types/node": "^17.0.17",
    "gts": "^3.1.0",
    "jest": "^27.5.1",
    "jest-coverage-thresholds-bumper": "^1.1.0",
    "ts-jest": "^27.1.3",
    "typescript": "^4.5.5"
  },
  "scripts": {
    "clean": "npm run clean --workspaces",
    "compile": "npm run compile --workspaces",
    "check": "gts check",
    "fix": "gts fix",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest ${1} --no-cache",
    "posttest": "jest-coverage-thresholds-bumper --silent",
    "release": "multi-semantic-release"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
