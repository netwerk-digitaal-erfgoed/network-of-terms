name: Nx migrate

on:
  pull_request:
    paths:
      - 'package.json'
      - 'package-lock.json'

jobs:
  migrate:
    uses: netwerk-digitaal-erfgoed/workflows/.github/workflows/nx-migrate.yml@main
    secrets: inherit
